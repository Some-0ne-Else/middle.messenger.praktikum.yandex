.chat-list{
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  margin: 20% auto 0;
}

.chat-list__header{
  margin: 0;
  font-family: $fontFamily;
  font-weight: 500;
  font-size: 40px;
  line-height: 40px;
  text-align: center;
  color: $defaultTextColor;
}
