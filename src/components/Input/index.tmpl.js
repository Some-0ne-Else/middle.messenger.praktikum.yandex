export const inputTemplate =`
<div class="input {{ className }}">
  <label for={{ name }} class="input__label">{{ label }}</label>
  <input class="input__input" id={{ name }} name={{ name }} type={{ type }} />
</div>`
